<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <packaging>pom</packaging>

    <parent>
        <groupId>cc.uncarbon.framework</groupId>
        <artifactId>helium-starters-parent</artifactId>
        <version>4.0.0-SNAPSHOT</version>
    </parent>

    <groupId>cc.uncarbon.module</groupId>
    <artifactId>helium-monolith</artifactId>
    <description>Helium 快速开发平台脚手架</description>
    <version>${revision}</version>

    <properties>
        <!-- 根据实际开发的 JDK 版本确定 -->
        <java.version>25</java.version>


        <!-- 当前工程内，所有Maven模块统一版本号 -->
        <revision>4.0.1-SNAPSHOT</revision>
        <bootstrap.version>${revision}</bootstrap.version>
        <commons.version>${revision}</commons.version>
        <admin-api.version>${revision}</admin-api.version>
        <app-api.version>${revision}</app-api.version>
        <sys.version>${revision}</sys.version>
        <file.version>${revision}</file.version>
        <tenant.version>${revision}</tenant.version>


        <!-- 固定其他第三方依赖版本 -->
        <!-- 密码哈希 -->
        <bouncycastle.version>1.84</bouncycastle.version>
        <!-- IP 地址归属地查询 -->
        <ip2region.version>3.3.7</ip2region.version>
        <!-- 文件上传 -->
        <x-file-storage.version>2.3.0</x-file-storage.version>
        <minio.version>8.5.17</minio.version>
        <s3-sdk-v1.version>1.12.787</s3-sdk-v1.version>
        <s3-sdk-v2.version>2.31.58</s3-sdk-v2.version>
        <aliyun-sdk-oss.version>3.18.5</aliyun-sdk-oss.version>
    </properties>

    <modules>
        <module>bootstrap</module>
        <module>integration-tests</module>
        <module>commons</module>
        <module>endpoints/admin-api</module>
        <module>endpoints/app-api</module>
        <module>modules/file-contract</module>
        <module>modules/file-impl</module>
        <module>modules/sys-contract</module>
        <module>modules/sys-impl</module>
        <module>modules/tenant-contract</module>
        <module>modules/tenant-impl</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- 其他第三方依赖 -->
            <!-- 密码哈希 -->
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>

            <!-- IP 地址归属地查询 -->
            <dependency>
                <groupId>org.lionsoul</groupId>
                <artifactId>ip2region</artifactId>
                <version>${ip2region.version}</version>
            </dependency>

            <!-- 文件上传 -->
            <!-- 对象存储工具类 https://gitee.com/dromara/x-file-storage -->
            <dependency>
                <groupId>org.dromara.x-file-storage</groupId>
                <artifactId>x-file-storage-spring</artifactId>
                <version>${x-file-storage.version}</version>
                <exclusions>
                    <!-- hutool 版本冲突，用框架统一的版本 -->
                    <exclusion>
                        <groupId>cn.hutool</groupId>
                        <artifactId>hutool-core</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <!-- Amazon S3 依赖包（SDK v1） -->
            <dependency>
                <groupId>com.amazonaws</groupId>
                <artifactId>aws-java-sdk-s3</artifactId>
                <version>${s3-sdk-v1.version}</version>
            </dependency>

            <!-- Amazon S3 依赖包（SDK v2） -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>s3</artifactId>
                <version>${s3-sdk-v2.version}</version>
            </dependency>

            <!-- 阿里云 OSS 依赖包 -->
            <dependency>
                <groupId>com.aliyun.oss</groupId>
                <artifactId>aliyun-sdk-oss</artifactId>
                <version>${aliyun-sdk-oss.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- 引入单元测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <!-- 支持 mybatis XML 放置在 java 包下 -->
        <resources>
            <resource>
                <directory>src/main/java</directory>
                <includes>
                    <include>**/*.xml</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>

        <plugins>
            <!-- 编译时，跳过单元测试 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>

            <!-- 编译时，不执行 mvn deploy -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>${maven-deploy-plugin.version}</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <!-- 编译时，不生成 javadoc.jar -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <!-- 编译时，不生成 sources.jar -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <configuration>
                    <skipSource>true</skipSource>
                </configuration>
            </plugin>

            <!-- 统一版本号，占位符 ${revision} -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.2.7</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
