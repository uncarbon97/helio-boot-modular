<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <parent>
        <groupId>cc.uncarbon.module</groupId>
        <artifactId>helium-monolith</artifactId>
        <version>${revision}</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>admin-api</artifactId>
    <version>${revision}</version>

    <properties>
        <!-- 引用下游服务 -->
        <!-- 系统管理 -->
        <sys.version>${revision}</sys.version>
        <!-- 文件管理 -->
        <file.version>${revision}</file.version>
        <!-- 租户管理 -->
        <tenant.version>${revision}</tenant.version>


        <!-- 引用其他第三方依赖 -->
    </properties>

    <dependencies>
        <!-- 引用下游服务 -->
        <dependency>
            <groupId>cc.uncarbon.module</groupId>
            <artifactId>sys-service</artifactId>
            <version>${sys.version}</version>
        </dependency>

        <dependency>
            <groupId>cc.uncarbon.module</groupId>
            <artifactId>file-service</artifactId>
            <version>${file.version}</version>
        </dependency>

        <dependency>
            <groupId>cc.uncarbon.module</groupId>
            <artifactId>tenant-service</artifactId>
            <version>${tenant.version}</version>
        </dependency>


        <!-- 引用其他契约 -->
        <dependency>
            <groupId>cc.uncarbon.module</groupId>
            <artifactId>commons</artifactId>
        </dependency>


        <!-- 引用框架基础包/启动包 -->
        <dependency>
            <groupId>cc.uncarbon.framework</groupId>
            <artifactId>helium-starter-base</artifactId>
        </dependency>

        <dependency>
            <groupId>cc.uncarbon.framework</groupId>
            <artifactId>helium-starter-openapi3</artifactId>
        </dependency>

        <dependency>
            <groupId>cc.uncarbon.framework</groupId>
            <artifactId>helium-starter-redis</artifactId>
        </dependency>

        <dependency>
            <groupId>cc.uncarbon.framework</groupId>
            <artifactId>helium-starter-satoken</artifactId>
        </dependency>

        <dependency>
            <groupId>cc.uncarbon.framework</groupId>
            <artifactId>helium-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>cc.uncarbon.framework</groupId>
            <artifactId>helium-starter-web-security</artifactId>
        </dependency>


        <!-- 引用其他第三方依赖 -->
    </dependencies>
</project>
